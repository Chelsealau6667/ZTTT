# 模版管理与主体配置原型交互说明

> 基于原型文件：[模版管理页面原型.html](模版管理页面原型.html)
> 生成日期：2026/07/27

---

## 一、页面整体结构

页面采用顶部 Tab 切换布局，包含两个一级 Tab：

| Tab 名称 | 对应内容 |
|:---|:---|
| **模版管理** | 形式发票模版的增删改查、状态切换、预览、下载、导入更新 |
| **主体配置** | 利润中心与模版之间的映射规则配置（即「在什么条件下使用哪个模版开票」） |

---

## 二、模版管理 Tab

### 2.1 列表区

| 列名 | 说明 |
|:---|:---|
| 模版编码 | 系统生成，格式 `XSFPMB` + 6 位自增数字 |
| 名称 | 上传文件时自动提取的文件名 |
| 状态 | 启用 / 禁用，通过 Toggle 开关切换 |
| 最近更新 | 上次导入/更新的时间 |
| 操作 | 预览、下载、导入更新 |

**Toolbar 操作**：
- 左上角显示「共 N 个模版」统计
- 「+ 新增模版」按钮：打开批量导入弹窗
- 右侧下拉筛选：按状态（全部/启用/禁用）过滤
- 搜索框：支持按编码/名称模糊搜索

### 2.2 状态切换（启用 ↔ 禁用）

点击 Toggle 开关时，**先恢复开关原状**，弹出二次确认框：

| 操作 | 确认文案 |
|:---|:---|
| 启用禁用模版 | 「确认启用模版 {编码}？启用后可在主体配置中选择此模版。」 |
| 禁用启用模版 | 「确认禁用模版 {编码}？禁用后已关联此模版的配置规则将失效，请检查主体配置。」（红色警示） |

用户点击「确认」后，才真正变更状态并刷新列表。

### 2.3 模版预览

点击「预览」打开全屏遮罩层，居中展示 A4 纸大小的发票排版预览：

| 模版编码 | 预览内容说明 |
|:---|:---|
| XSFPMB000001 / 000002 / 000003 / 000004 | 加拿大 CA 模板（含 GST/HST 税率差异） |
| XSFPMB000005 / 000006 / 000007 / 000008 | 温哥华 VC 模板（含 GST/HST 税率差异） |
| XSFPMB000009 / 000010 / 000011 / 000012 | 美国 US 模板（EL/GA 公司差异，含正负数） |
| XSFPMB000013 | 日本 JP 模板 |
| XSFPMB000014 | 澳洲 AU 模板 |
| XSFPMB000015 / 000016 | ABU 模板（深圳/香港公司差异） |

预览区域外点击可关闭。

### 2.4 下载

点击「下载」触发对应模版文件的下载（.xlsx）。

### 2.5 导入更新

针对单个模版的更新流程：点击「导入更新」后打开与「新增模版」相同的弹窗，但标题变为「导入更新 - {模版编码}」。

---

## 三、主体配置 Tab

### 3.1 列表区

| 列名 | 说明 |
|:---|:---|
| 利润中心编码 | 格式如 `200000` |
| 公司名称 | 如「加拿大」「美国EL」等 |
| 税率条件 | 支持多税率组合，如 `5%,13%` |
| 金额类型 | 正数 / 负数 / 全部（带颜色标签） |
| 适用模版 | 显示模版编码（等宽字体蓝色） |
| 操作 | 编辑、删除 |

**Toolbar 操作**：
- 「+ 新增配置」：打开单条新增弹窗
- 「新增导入」：打开批量导入弹窗

### 3.2 查询条件区

位于列表上方，横向排列：

| 查询条件 | 组件 | 说明 |
|:---|:---|:---|
| **利润中心编码** | 多选下拉框 | 展示 `编码 - 名称`，支持通过编码或名称模糊搜索 |
| **适用模版编码** | 多选下拉框 | 展示 `模版编码 - 模版名称`，支持模糊搜索 |
| **查询** | 按钮 | 按已选条件过滤列表 |
| **重置** | 按钮 | 清空所有查询条件，恢复展示全部数据 |

**多选下拉交互**：
- 点击输入框展开下拉面板，显示全部候选项
- 顶部含搜索框，输入关键词实时过滤选项
- 选中项以胶囊标签形式展示在输入框内，点击标签右侧 `×` 可移除
- 点击面板外部区域收起下拉

---

## 四、弹窗交互详细说明

### 4.1 模版导入弹窗（新增模版 / 导入更新）

弹窗标题根据场景变化：「新增模版」或「导入更新 - {编码}」。

两种场景的上传流程均简化为**直接提示结果**，不展示差异对比列表，不二次确认。

| 场景 | 上传后行为 | 提示文案 |
|:---|:---|:---|
| **新增模版** | 选择文件后关闭弹窗，直接轻量提示成功 | 「模版导入成功」 |
| **导入更新** | 选择文件后关闭弹窗，直接轻量提示成功/失败 | 「模版更新成功」/「更新失败，请稍后重试」 |

**轻量提示弹窗样式**：
- 白底居中卡片，顶部绿色圆圈对勾图标
- 中间单行文字提示（如「上传成功」）
- 底部一个蓝色「确定」按钮，点击后关闭

> 注：该提示样式与系统统一的标准提示弹窗保持一致。

### 4.2 配置编辑弹窗（新增 / 编辑）

弹窗为中等尺寸，包含以下表单字段：

| 字段 | 组件 | 必填 | 说明 |
|:---|:---|:---:|:---|
| 主体 | 下拉选择 | ✓ | 利润中心列表，如 `200000 - 加拿大` |
| 税率条件 | 多选下拉 | - | 候选项：`0%`、`5%`、`13%`、`14%`、`15%`、`全部`；不选则默认「全部」 |
| 金额类型 | 下拉选择 | - | `正数` / `负数` / 空（默认全部） |
| 适用模版 | 下拉选择 | ✓ | 根据「税率条件+金额类型」联动过滤可用模版；需先选择条件才显示候选项 |

**联动规则**：
- 选择金额类型后，适用模版下拉框自动过滤：
  - 「正数」→ 排除以 `-N` 或 `N` 结尾的负数模版
  - 「负数」→ 仅保留以 `-N` 或 `N` 结尾的负数模版
- 适用模版仅展示状态为「启用」的模版

**保存校验**：
- 主体、适用模版为必填
- 校验 (利润中心编码, 税率条件排序后, 金额类型) 三维度唯一性
- 重复时弹窗提示「该条件组合已存在配置，请先删除原有配置」

### 4.3 配置删除

点击「删除」弹出二次确认：「确认删除此配置规则？删除后该条件组合将无法匹配到模版，开票时可能报错。」确认后删除并刷新列表。

### 4.4 主体配置导入弹窗

弹窗标题「新增导入」，同样为四步流程：

| 步骤 | 界面元素 | 交互说明 |
|:---|:---|:---|
| **Step 1 上传** | 拖拽上传区 + 文件选择按钮 + 下载导入模版按钮 | 支持 `.xlsx` / `.csv`；底部提供「下载导入模版」入口及格式提示 |
| **Step 2 差异对比** | 变更清单表格（含重复检测） | 展示新增/跳过两种状态；自动检测文件中重复行及与系统存量重复的行 |
| **Step 3 导入中** | 进度条 + 百分比文字 | 同模版导入 |
| **Step 4 完成** | 成功/跳过统计 | 同模版导入 |

**导入模版 CSV 格式**：

```
利润中心编码,税率条件,金额类型,适用模版
200000,5%,13%,正数,XSFPMB000001
200001,全部,全部,XSFPMB000005
```

> 多税率用英文逗号分隔；金额类型为空时系统默认「全部」。

---

## 五、核心数据模型（原型 Mock）

### 5.1 模版数据

```js
const templates = [
  {id:'XSFPMB000001', name:'FBU-加拿大-5%GST', status:'enabled', time:'2026-06-22 10:30'},
  {id:'XSFPMB000002', name:'FBU-加拿大-5%GST-负数', status:'enabled', time:'2026-06-22 10:30'},
  // ... 共 16 条
];
```

### 5.2 主体配置数据

```js
const configs = [
  {id:1, entityCode:'200000', entityName:'加拿大', tax:['5%','13%'], sign:'正数', template:'XSFPMB000001'},
  {id:2, entityCode:'200000', entityName:'加拿大', tax:['5%','13%'], sign:'负数', template:'XSFPMB000002'},
  // ... 共 14 条
];
```

### 5.3 利润中心选项

```js
const ENTITY_OPTIONS = [
  {code:'200000', name:'加拿大'},
  {code:'200001', name:'温哥华'},
  {code:'200002', name:'美国EL'},
  {code:'200003', name:'美国GA'},
  {code:'200004', name:'日本'},
  {code:'200005', name:'澳洲'},
  {code:'200006', name:'深圳'},
  {code:'200007', name:'香港'},
];
```

---

## 六、与需求文档的对应关系

| 需求章节 | 原型实现 |
|:---|:---|
| 9.4.2.1 模版编码规则生成 | 列表中展示 `XSFPMB000001` 等编码 |
| 9.4.2.1 状态切换（启用/禁用） | Toggle 开关 + 二次确认弹窗 |
| 9.4.2.2 数据行维度 | 一条配置 = 利润中心 + 税率条件 + 金额类型 → 适用模版 |
| 9.4.2.2 查询条件（多选下拉 + 标签） | 利润中心编码、适用模版编码均使用多选下拉组件 |
| 9.4.2.2 查询/重置按钮 | 独立按钮触发，非实时过滤 |
| 9.4.2.2 导入校验（五步校验链） | 原型中实现为 4 步流程 + 差异对比展示重复检测 |
| 9.4.2.2 唯一性校验 | 新增/编辑时前端校验三维度重复 |
| 9.4.2.2 匹配规则 | 原型中配置列表按 entityCode + tax + sign 存储 |
| 9.4.2.2 模版状态联动 | 金额类型选择后联动过滤适用模版列表 |
